# Generated by Django 5.2.16 on 2026-08-04 05:02

import accounts.models
from django.db import migrations


def repair_superuser_roles(apps, schema_editor):
    """Make every existing superuser a full Admin.

    Before this migration, ``createsuperuser`` produced a superuser whose
    ``role`` defaulted to ``volunteer`` (the model default) — a volunteer-role
    superuser that fails the ``role == admin`` gates on Admin-only views. Flip
    any such row to ``role=admin`` and ensure ``is_staff`` is set, so the
    invariant ("an Admin is is_staff + is_superuser + role=admin") holds for
    data created before the custom manager existed. Idempotent.
    """
    User = apps.get_model("accounts", "User")
    User.objects.filter(is_superuser=True).update(role="admin", is_staff=True)


def do_nothing(apps, schema_editor):
    """No reverse: restoring a volunteer-role superuser is never desirable."""
    pass


class Migration(migrations.Migration):

    dependencies = [
        ('accounts', '0001_initial'),
    ]

    operations = [
        migrations.AlterModelManagers(
            name='user',
            managers=[
                ('objects', accounts.models.UserManager()),
            ],
        ),
        migrations.RunPython(repair_superuser_roles, reverse_code=do_nothing),
    ]
